package game;
/*
 * this will be used as an animation to liven up the game play
 * the shot will take the start destination and the end destination
 * it must be called in update and will update all shots pending
 * if certain ships have specific shot patterns, multiple shots will need to be updated
 * a shot will be created each time a new start and end destination is given
 * when a shot reaches the destination, it is deleted. but not before notifying the
 * board that it has landed.
 */
public class Shot
{

}
